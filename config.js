var config = {
  // instance
  localhost: {
    // config
    server: {
      // hostname or ip address
      address: 'ipaddress'
    },
    // rcon
    rcon: {
      // rcon port
      port: 'rconport',
      // rcon password
      password: 'password'
    }
  }
};


module.exports = config;
